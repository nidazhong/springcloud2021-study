server:
  port: 3377
spring:
  application:
    name: nacos-client3377
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml

## 注意nacos的DataId为 nacos-client3377-dev.yml